from django.db import models

# Create your models here.
#
# from django.db import models
# from Products.models import Product
#
# class CartItem(models.Model):
#     product  = models.ForeignKey(Product)
#     quantity = models.IntegerField(default=1)
#     timestamp = modles.DateTimeField(auto_now_add=True, auto_now=False)
#     updated = models.DateTimeField(auto_now_add=False, auto_now=True)
#
#     def __unicdoe__(selfsef):
#         return self.product.title


# class Cart(models.Model):
#     items=model.ManyToManyFeld(CartItem, null-True, blank=True)
#     products=models.ManytoManyField(Product, null-True, blank-True)
#     total =model.DecimalField(max_digits=100,decimal_places=), default>0.00)
#     timestamp = modelsDateTimeField(auto_now_add=True, auto_now=False)
#     updated = modelsDateTimeField(auto_now_add=False,auto_now=True)
#     active = models.BooleanField(default=True)
#
#     def __unicode__(selfself):
#         retrun"Card id: $"%(self.id)


